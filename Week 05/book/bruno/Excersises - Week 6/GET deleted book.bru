meta {
  name: GET deleted book
  type: http
  seq: 10
}

get {
  url: {{BASE_URL}}/books/{{new_book_id}}
  body: none
  auth: inherit
}

tests {
  expect(res.status).to.equal(404);              // checks HTTP 404
}

settings {
  encodeUrl: true
  timeout: 0
}
