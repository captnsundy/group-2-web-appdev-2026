meta {
  name: GET non-existing book
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/books/772
  body: none
  auth: inherit
}

tests {
  expect(res.status).to.equal(404);
}

settings {
  encodeUrl: true
  timeout: 0
}
